import React from "react";

const NavBar = () => {
  return (
    <nav className="nav">
      <a className="nav-link active" href="/">
        Employee Directory
      </a>
    </nav>
  );
};

export default NavBar;